name: Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  migrations:
    name: PostgreSQL migration smoke
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: wppconnect
          POSTGRES_PASSWORD: test-password
          POSTGRES_DB: wppconnect
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U wppconnect -d wppconnect"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      PGPASSWORD: test-password
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 'latest'

      - name: Install backend dependencies
        working-directory: backend
        run: bun install --frozen-lockfile

      - name: Apply complete migration chain
        run: |
          for migration in backend/migrations/*.sql; do
            echo "Applying ${migration}"
            psql --set ON_ERROR_STOP=1 \
              --host 127.0.0.1 --username wppconnect --dbname wppconnect \
              --file "${migration}"
          done

      - name: Assert platform schema
        run: |
          psql --set ON_ERROR_STOP=1 \
            --host 127.0.0.1 --username wppconnect --dbname wppconnect <<'SQL'
          SELECT 1 / CASE WHEN COUNT(*) = 6 THEN 1 ELSE 0 END FROM products;
          SELECT 1 / CASE WHEN COUNT(*) = 7 THEN 1 ELSE 0 END FROM product_plans;
          SELECT 1 / CASE WHEN to_regclass('public.auth_sessions') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.usage_events') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.extension_licenses') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.media_jobs') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.telemetry_snapshots') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.catalog_sync_runs') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.compatibility_manifests') IS NOT NULL THEN 1 ELSE 0 END;
          SELECT 1 / CASE WHEN to_regclass('public.extension_license_billing_events') IS NOT NULL THEN 1 ELSE 0 END;
          SQL

      - name: Exercise migration from the legacy Cloud schema
        run: bash .github/scripts/legacy-cloud-migration-smoke.sh

      - name: Exercise the control-plane interfaces
        run: bash .github/scripts/platform-smoke.sh

  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

  backend:
    name: Backend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 'latest'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bunx tsc --noEmit

      - name: Unit tests
        run: bun test

  license-sdk:
    name: License SDK tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/license-sdk
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - run: npm test

  telemetry-sdk:
    name: Telemetry SDK tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/telemetry-sdk
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - run: npm test

  media-api:
    name: Media API tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/media-api
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 'latest'
      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun test
      - name: Build production image
        working-directory: .
        run: docker build -t wppconnect-media-api ./services/media-api
